import React, { useState } from 'react';

function CustomArgumentModal({ open, onCancel, onSubmit }) {
  return (
    <div>
      <p>Debugging CustomArgumentModal</p>
    </div>
  );
}

export default CustomArgumentModal;